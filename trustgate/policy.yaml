# Policy loaded from Trust Gate server
version: "nist_balanced@1.0"
name: "nist_balanced"
profile: "nist_balanced"

thresholds:
  vrs_require_human: 0.6
  vrs_block: 0.85

roles:
  read_only:
    allowed_tools: ["*"]
roles:
  operator:
    allowed_tools: ["*"]
roles:
  trusted:
    allowed_tools: ["*"]

# SHA-256: 1231f5637be0bb5739a145188a82786d2101bb5fd80977e229660b2471db916c
# Dangerous patterns: 6 rules active
