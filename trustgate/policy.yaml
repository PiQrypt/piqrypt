# PiQrypt TrustGate — Policy v1.0
# Seuils adaptes aux VRS de la demo (~0.40-0.85)
#
# vrs_require_human : en dessous -> decision REQUIRE_HUMAN (queue dashboard)
# vrs_block         : en dessous -> decision BLOCK

version: "1.0"
name: default

thresholds:
  vrs_require_human: 0.80
  vrs_block: 0.25
  tsi_unstable_action: REQUIRE_HUMAN
  tsi_critical_action: BLOCK
